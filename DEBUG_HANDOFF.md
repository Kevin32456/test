# DEBUG_HANDOFF

## 錯誤現象

狗從持球者身旁掠過後，會沿原方向衝到遠處；同時狗頭永遠朝速度方向，無法呈現「頭轉回玩家、身體仍往外滑」的甩尾。

## 最小重現步驟

1. 開始對局並持球。
2. 近距離繞狗轉向，讓狗從玩家身旁掠過。
3. 觀察狗是否立即面向玩家，並在短距離慣性滑行後折返。

## 目前錯誤訊息 / Log

無執行期錯誤；屬於追擊物理與視覺朝向耦合問題。

## 已嘗試修法與證據

- 單純降低轉向與側向摩擦：甩尾增加，但狗會追丟。
- 提高轉向、抓地與追回速度：較會追球，但甩尾感消失。
- 依距離切換繞圈／追回參數：仍因朝向綁定速度，掠過後狗頭不會先轉回玩家。

## 失敗原因

`updateDogToward` 直接旋轉速度向量，且每 tick 把 `dog.angle` 設為速度角度；單靠數值無法同時表現朝向與慣性滑移。

## 根因假設

將視覺朝向與速度向量分離，並在目標位於速度後方時對速度施加反向牽引，可讓狗快速面向玩家、短暫向外滑後回追，而不會持續衝遠。

## 下一個可否證驗證步驟

已實作獨立朝向與急回頭牽引；`npm run typecheck` 通過，瀏覽器可正常載入大廳。下一步由玩家用相同近距離繞圈步驟確認：

- 狗頭是否在約 0.2 秒內轉回玩家。
- 狗身是否只沿舊方向滑行約一個身位內便折返。
- 折返後是否能持續追球，不再衝至場地外圈。

最新調校：玩家回報急轉仍稍滑，`DOG_REVERSE_GRIP` 已由 4.2 提高至 5.5，只縮短急回頭後滑，不改一般繞圈參數。

## 不准再重複的修法

不要再只靠同一組 `TURN_RATE`、`FORWARD_GRIP`、`LATERAL_FRICTION` 全域數值來交換「甩尾」與「不追丟」。
